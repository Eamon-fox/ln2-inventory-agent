# Agent 文档兼容入口

本文件保留用于兼容旧链接，不作为权威入口，也不维护独立规则（避免多套 agent 约束分叉）。

## 权威入口

以仓库根目录的 `CLAUDE.md` 为权威工作入口。根目录 `AGENTS.md` 与其同源，仅作指针。

## 架构必读文档

1. `../CLAUDE.md`
2. `00-约束模型.md`
3. `01-系统架构总览.md`
4. `02-模块地图.md`
5. `03-共享瓶颈点.md`

## 说明

- `CLAUDE.md` 承担环境、运行、测试命令与改动边界说明。
- `tests/RUNBOOK.md` 承担测试执行说明。
