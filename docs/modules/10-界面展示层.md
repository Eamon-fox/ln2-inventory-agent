# 界面展示层

模块 ID：`gui_presentation`

本页默认按软约束和背景说明理解。真正硬约束只以 `docs/01-系统架构总览.md`、`docs/02-模块地图.md`、`docs/03-共享瓶颈点.md` 的机器可读契约为准。

## 模块定位

本模块负责所有直接面向用户的 Qt 展示与交互组件。它的职责是展示状态、收集输入、触发稳定入口，不负责定义库存规则、导入策略或工具契约。

如果一个改动的目标是“让界面显示正确、交互顺手、输入可控”，它大概率属于本模块。

## 拥有路径

- `app_gui/ui/`

## 主职责

- 面板、对话框、表格、列表、控件布局
- 面板内局部交互状态
- 用户输入约束与前端提示
- 面板级信号定义与视觉反馈

## 当前主要协作面（软约束）

跨模块调用 `OperationsPanel` 时，只允许依赖以下公共方法：

- `apply_meta_update`
- `set_migration_mode_enabled`
- `update_records_cache`
- `set_prefill`
- `set_prefill_background`
- `set_add_prefill`
- `set_add_prefill_background`
- `add_plan_items`
- `execute_plan`
- `clear_plan`
- `reset_for_dataset_switch`
- `on_export_inventory_csv`
- `emit_external_operation_event`
- `print_plan`
- `print_last_executed`
- `on_undo_last`
- `remove_selected_plan_items`
- `on_plan_table_context_menu`
- `refresh_plan_store_view`
- `OverviewPanel.bind_plan_store`
- `OverviewPanel.refresh_plan_store_view`
- `AIPanel.apply_runtime_settings`
- `AIPanel.runtime_settings_snapshot`
- `AIPanel.has_running_task`

除这些入口外，不允许跨模块直接依赖 `OperationsPanel` 的私有方法、临时字段或子控件结构。

## 允许依赖

- `app_gui/application/` 暴露的稳定流程入口
- `app_gui/plan_*.py` 的结果模型或只读协作能力
- `lib/` 中纯展示辅助能力，例如位置格式化、只读解析或文本格式化

## 禁止依赖

- `lib.tool_api_write*`
- `lib.tool_api_write_validation`
- `agent/`
- 把库存写入规则、审计规则、导入规则直接写进 UI 组件

## 与其他模块的边界

### 与 `gui_application` 的边界

- 展示层负责“显示什么按钮、输入什么值、触发什么稳定调用”
- 应用层负责“这次操作要怎么编排、怎么跨面板同步、怎么发布事件”

### 与 `inventory_core` 的边界

- 展示层可以显示来自核心层的数据
- 展示层不应决定核心字段规则、工具契约、写入顺序或审计策略

### 与 `agent_runtime` 的边界

- 展示层只能展示 agent 的结果和事件
- 不要把 agent 的推理、恢复策略、工具选择逻辑塞回 UI

## 共享瓶颈点

以下文件虽然经常与展示层任务相关，但不属于本模块可自由改动的内部文件：

- `app_gui/main.py`
- `app_gui/main_window_flows.py`
- `app_gui/tool_bridge.py`
- `app_gui/i18n/translations/en.json`
- `app_gui/i18n/translations/zh-CN.json`

一旦任务需要改这些文件，就按跨模块任务处理。

## 适合的任务切分方式

适合并行拆分为：

- 单个面板内的布局重构
- 某个对话框或表格的交互细化
- 某个 UI 子模块的视觉与输入限制调整

不适合并行拆分为：

- 多个任务同时改同一个面板的公共方法签名
- 一边改 UI，一边改主窗口 wiring

## 典型反模式

- 为了“先能跑”，在 `app_gui/ui/` 里直接写工具调用编排
- 让 UI 组件直接导入底层写校验模块
- 依赖另一个面板的私有字段或私有方法
- 在 UI 层复制业务规则，导致 GUI 与 agent 行为不一致

## 最小测试责任

- `pytest -q tests/integration/gui`

如果只改某个具体面板，至少补跑对应的 GUI 集成测试文件。
